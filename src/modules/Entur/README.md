# Entur

## Using the module

To use this module, add the following configuration in the App.js file:
```
<Entur
        area="3 / 1 / 7 / 5"
        location={[0, 0]}
        maxDistance={500}
        accessToken=""
      />
```
